export function fizzbuzz() { let str = ""; for (let i = 1; i <= 100; i++) { str += (i % 3 === 0 && i % 5 === 0) ? "FizzBuzz\n" : (i % 3 === 0 ? "Fizz\n" : (i % 5 === 0 ? "Buzz\n" : `${i}\n`)); } return str; }